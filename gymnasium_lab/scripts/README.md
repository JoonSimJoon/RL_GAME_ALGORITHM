# Scripts

이 폴더는 두 가지 용도로 사용합니다.

1. 필요하면 `scripts/train.py` 같은 얇은 래퍼 스크립트를 둡니다.
2. 수업용 대본, 데모 메모, 진행자 치트시트 같은 보조 자료를 둡니다.

현재 기준으로 실행은 아래 두 방식 중 하나를 사용할 수 있습니다.

1. `python -m rl_lab.runners.train` 같은 모듈 실행 방식
2. `scripts/train.py` 같은 얇은 진입 스크립트 방식

초기 구현에서는 중복을 줄이기 위해 모듈 실행 방식을 우선 추천합니다.
